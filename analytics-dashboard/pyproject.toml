[project]
name = "tdd-analytics-dashboard"
version = "1.0.0"
description = "Local Streamlit dashboard for Interactive TDD usage analytics"
requires-python = ">=3.11"
dependencies = [
    "streamlit[snowflake]>=1.56.0",
]
